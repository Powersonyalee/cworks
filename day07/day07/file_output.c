/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

	FILE* fp;
	fp = fopen("hello.txt", "w"); // 쓰기모드 - "w", "wt" 도 사용 가능

	// 쓰기함수 -fputc() 문자 1개, fputs() -문자열
	fputc('H', fp);
	fputc('E', fp);
	fputc('L', fp);
	fputc('L', fp);
	fputc('0', fp);
	fputc('\n', fp); // 개행문자 - 줄바꿈
	

	fclose(fp);

	return 0;
}

*/
